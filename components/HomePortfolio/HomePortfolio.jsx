import { Container, GalleryList, Section } from '@/components';

export const HomePortfolio = () => {
  return (
    <Section>
      <Container>
        <GalleryList />
      </Container>
    </Section>
  );
};
